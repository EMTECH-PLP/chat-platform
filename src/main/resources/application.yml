spring:
  application:
    name: chat-platform
  config:
    import: classpath:application-postgres.yml

server:
  port: 8080
  servlet:
    context-path: /api

springdoc:
  swagger-ui:
    path: /docs
