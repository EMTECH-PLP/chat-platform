spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/chat_platform}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Mikemitch!2017}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: ${SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true